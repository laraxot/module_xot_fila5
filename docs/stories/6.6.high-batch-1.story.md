# BMAD Story — High Batch 1 Fix (Quotation, User, Tenant, Media)

## Understand
- **Moduli**: Quotation (404), User (272), Tenant (239), Media (237)
- **Total errori**: 1152
- **Tipologia**: Stimata - model types, relationship, mixed cast
- **Regola**: `mixed` -> tipi concreti; User -> UserContract/XotData

## Plan
1. Analizzare errori per tipo in ogni modulo
2. User model è critico - potrebbe avere pattern ripetuti negli altri
3. Tenant e Media sono modulo business
4. Verificare con phpstan + git sync

## Implement

### Step 1: Analisi User (272 errori)
```bash
./vendor/bin/phpstan analyse Modules/User --error-format=table 2>&1 | head -100
```

### Step 2: Analisi Tenant (239 errori)
```bash
./vendor/bin/phpstan analyse Modules/Tenant --error-format=table 2>&1 | head -100
```

### Step 3: Analisi Media (237 errori)
```bash
./vendor/bin/phpstan analyse Modules/Media --error-format=table 2>&1 | head -100
```

### Step 4: Analisi Quotation (404 errori)
```bash
./vendor/bin/phpstan analyse Modules/Quotation --error-format=table 2>&1 | head -100
```

## Verify
- [ ] User: < 50 errori
- [ ] Tenant: < 50 errori
- [ ] Media: < 50 errori
- [ ] Quotation: < 100 errori
- [ ] Git sync completato

## Status
- [ ] Da iniziare
