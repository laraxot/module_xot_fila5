# BMAD Story — Pest Coverage Campaign

## Understand
- **Obiettivo**: Incrementare Pest coverage per tutti i moduli
- **Regola**: Coverage >= 70% per ogni modulo
- **Strumento**: `./vendor/bin/pest --coverage`

## Plan
1. Verificare coverage attuale per modulo
2. Identificare aree senza test
3. Creare test per coverage incrementale
4. Git sync per ogni modulo

## Implement

### Step 1: Check Coverage
```bash
./vendor/bin/pest --coverage --min=70
```

### Step 2: Coverage per modulo
```bash
for m in Modules/*/; do
    echo "=== $(basename $m) ==="
    ./vendor/bin/pest tests/Modules/$(basename $m)/ --coverage 2>/dev/null | grep "Coverage"
done
```

### Step 3: Create Tests
```php
test('example test for coverage', function () {
    // Test logic here
});
```

## Verify
- [ ] Coverage medio >= 70%
- [ ] Nessun test rotto
- [ ] Git sync completato

## Status
- [ ] Da iniziare
