# BMAD Story — Critical Batch 1 Fix (Timber, Intervention, Wts)

## Understand
- **Moduli**: Timber (1210), Intervention (558), Wts (494)
- **Total errori**: 2262
- **Tipologia**: Stimata - seeder factory, mixed cast, return type
- **Regola**: `mixed` -> tipi concreti; cast sicuri

## Plan
1. Analizzare errori per tipo in ogni modulo
2. Identificare pattern comuni
3. Applicare fix sistematici
4. Verificare con phpstan + phpmd + phpinsights + pest
5. Git sync per ogni modulo

## Implement

### Step 1: Analisi Timber
```bash
./vendor/bin/phpstan analyse Modules/Timber --error-format=table 2>&1 | head -100
```

### Step 2: Analisi Intervention
```bash
./vendor/bin/phpstan analyse Modules/Intervention --error-format=table 2>&1 | head -100
```

### Step 3: Analisi Wts
```bash
./vendor/bin/phpstan analyse Modules/Wts --error-format=table 2>&1 | head -100
```

### Expected Patterns
- Seeder con factory mancanti
- Mixed cast in queries
- Return type mancanti

## Verify
- [ ] Timber: < 100 errori
- [ ] Intervention: < 50 errori
- [ ] Wts: < 50 errori
- [ ] Git sync completato

## Status
- [ ] Da iniziare
