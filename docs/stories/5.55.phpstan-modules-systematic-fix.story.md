# BMAD Story — PHPStan Systematic Fix (Modules)

## Understand
- Problema: PHPStan bootstrap fallisce (`AdminPanelProvider.php` linea 10, conflitto in `XotBaseMainPanelProvider.php`).
- Regole: `phpstan.neon` non si modifica; `mixed` sostituito con tipi opportuni; `User` -> `UserContract` / `XotData`; `docs/` aggiornati.

## Plan
1. Fix conflitti git in `Modules/Xot/app/Filament/Traits/HasXotTable.php`
2. Verifica bootstrap PHPStan
3. Per ogni modulo: `git fetch laraxot` + `merge` + `add -A` + `commit`
4. Eseguire `phpstan`, `phpmd`, `phpinsights`, `pest`
5. Aggiornare docs modulo + docs tema

## Implement
- Fix conflitti in `XotBaseMainPanelProvider.php`, `HasXotTable.php`
- Eseguire analisi modulo per modulo
- Sostituire `mixed` con tipi concreti dove possibile
- Passare per `XotData` quando si usa `User`

## Verify
- `php -l` su file modificati
- `./vendor/bin/phpstan analyse <file>` o `Modules/`
- `./tools/phpmd.sh <file>`
- `./vendor/bin/phpinsights analyse <file>`
- `./vendor/bin/pest <tests>`

## Document
- Aggiornare `docs/bmad-method.md`
- Creare/aggiornare `docs/stories/` per ogni modulo modificato
- Backlink tra docs

## Status
- Branch: `fix/phpstan-issues`
- Xot: conflitto fixato (`XotBaseMainPanelProvider.php` riscritto, `HasXotTable.php` da sistemare)
- Next: sincronizzare `Modules/Cms`, `Employee`, `Lang`, `Geo`, `UI`, `Notify`
=== BMAD STORY UPDATE (needs validation) ===
Task: Fix duplicate safeEloquentCastFixture() in helpers.php (bootstrap failure)
Status: PENDING VALIDATION by other AI agents
Next: Apply if (!function_exists()) guard
=== BMAD STORY UPDATE ===
Fix applied: helpers.php function_exists guards
New issue: Larastan internal error on SignPriceListResource.php (version incompatibility)
Status: DOCS UPDATED - awaiting resolution of Larastan bug
=== BMAD UPDATE: Larastan Internal Error ===
Bug URL: https://github.com/larastan/larastan/issues/new?template=bug-report.md
File affected: Modules/Lang/tests/Unit/LangCoverageGapsTest.php
Action: Reported as internal bug, not module code error
Regola: MAI downgrade; sempre ultima versione; aggiorno constraint a ^3.11.0
Bug Larastan: isForwardedQueryBuilderMethod() - documentato con URL bug report
File: Modules/Lang/tests/Unit/LangCoverageGapsTest.php (no syntax error - bug esterno)
