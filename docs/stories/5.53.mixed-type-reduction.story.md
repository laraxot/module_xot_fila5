---
story_id: "5.53"
title: Riduzione tipi mixed nei Modules (sostituzione con tipi adeguati)
type: refactor
status: in-progress
created: 2026-09-03
agents: [dev, qa]
related:
  - 5.52.committed-conflict-markers-xot-notify.story.md
  - 17.14.phpstan-modules-443-errors-cleanup.story.md
---

# Story 5.53 — Riduzione `mixed` nei Modules

## Contesto

Censimento 2026-09-03: 217 file PHP (esclusi test/build/vendor) usano `mixed`
come tipo nativo (`: mixed`, `mixed $x`). `mixed` disattiva l'analisi statica
nel punto d'uso e propaga `mixed` a valle: PHPStan L10 lo segnala come
technical debt.

## Principio (SSoT)

`mixed` è **legittimo solo al boundary**: input esterni non tipizzabili
(request, JSON, config, cast Eloquent, callable generici da framework).
Le `Safe*CastAction` di Xot esistono esattamente per convertire quel `mixed`
in tipi sicuri: **loro devono accettare `mixed`** — è il loro contratto.

`mixed` è **pigro** (da sostituire) quando:
1. Il valore ha già un tipo noto nel flusso (sostituire col tipo reale).
2. È un union esprimibile: `int|string`, `array|Collection`, ecc.
3. È un template Filament/Eloquent esprimibile con generics in PHPDoc.
4. È un return che il chiamante deve ri-castare subito (code smell).

## Acceptance Criteria

- [ ] AC1 — Triage completo: ogni file del censimento marcato KEEP (boundary)
      o FIX (sostituibile), con motivo.
- [ ] AC2 — Tutti i file FIX modificati: `mixed` → tipo adeguato
      (union, generics, tipo concreto o Safe*CastAction al boundary).
- [ ] AC3 — Gate completo dopo OGNI file: PHPStan (max) + phpmd + phpinsights
      + Pest pertinente (`./tools/post-edit-php.sh`).
- [ ] AC4 — Coverage Pest non in regressione: test aggiunti/migliorati
      per i file toccati privi di test.
- [ ] AC5 — Baseline PHPStan non peggiorata: `phpstan analyse Modules` a fine
      story con errori <= baseline di partenza.
- [ ] AC6 — Regola distillata nel second brain (wiki rules) + memoria.

## Rischi

- Cambiare firma pubblica di metodi usati da altri moduli → rompere chiamanti:
  verificare usi con grep prima di ogni cambio firma.
- Filament/Eloquent richiedono firme compatibili con i parent: dove il parent
  dichiara `mixed`, il figlio NON può restringere (LSP) → KEEP con PHPDoc
  più preciso (`@param`).

## Triage (2026-09-03)

### KEEP — boundary legittimo

| File | Motivo KEEP |
|---|---|
| `Xot/app/Actions/Cast/Safe*CastAction.php` | Contratto cast: accetta `mixed` al boundary, emette tipo sicuro |
| `Geo/app/Models/ComuneJson.php` | Closure validazione Laravel: firma `mixed $value` imposta dal framework |
| `Xot/app/Exports/QueryExport.php` | Interfaccia `WithMapping<mixed>` Maatwebsite: firma `map(mixed $row)` obbligata |
| `User/app/Actions/Shield/ShieldUtilsAction.php` | Closure `.map(static fn(mixed $item)...)` su Collection generica dopo array_keys: boundary `mixed` array PHP |
| Closures `dehydrateStateUsing` in Filament Forms | Il framework firma `fn(mixed $state)`: il chiamante è Filament, non noi |
| Closure `fn(mixed $_nome, int|string $code)` in ComuneJson | Argomento scartato (underscore), iterazione `each` su Collection mista |

### FIX — mixed pigro da sostituire

| File | Occorrenze | Sostituzione |
|---|---|---|
| `User/app/Filament/Pages/SocialiteProviderSettingsPage.php` | `maskSecret(mixed)`, `resolveSecret(mixed, mixed)`, `providerData(mixed)`, `stringList(mixed)` | `string\|null` per i parametri secret/state privati (già narrowati subito con `is_string`) |
| `User/app/Filament/Clusters/Passport/Resources/*/Pages/View*.php` | tabella Passport | Eloquent model attributes tipizzabili con `string\|int\|null` |
| `Xot/helpers/Helper.php` | `dddx(mixed $params)`, `post/put/patch(mixed $data)` | `mixed` per dddx è accettabile (debug); `$data` → `array<string,mixed>\|string` |
| `Activity/app/Filament/Pages/ListLogActivities.php` | cast mixed → string | `(string)` → tipo stringa |
| `Activity/app/Models/Activity.php` | proprietà schemaless | `array<string,mixed>` già corretto in PHPDoc; rimuovere `mixed` nativo |

## Log

- 2026-09-03: censimento iniziale 217 file (`.qwen/tmp/mixed-census.txt`).
- 2026-09-03: triage prima wave — 6 cluster KEEP identificati, 5 cluster FIX. Fix in corso con gate dopo ogni file.
