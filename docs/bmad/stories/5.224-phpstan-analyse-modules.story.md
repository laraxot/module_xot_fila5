---
id: "Xot/5.224-phpstan-analyse-modules"
title: "phpstan analyse Modules — cache fredda e zero errori"
status: done
module: Xot
created: 2026-09-23
related:
  - ../../wiki/troubleshooting/phpstan-modules-fix.md
  - ../../phpstan-status.md
  - ../../../../../docs/wiki/memories/phpstan-neon-immutable-agents.md
  - ../../../../../docs/wiki/memories/no-tests-on-production-10-100-200-15.md
qmd: "phpstan analyse Modules level max neon immutabile zero errori fleet"
---

# Story 5.224 — `phpstan analyse Modules`

## Perché

Utente: `cd laravel && ./vendor/bin/phpstan analyse Modules` e sistema.
Gate: livello max da `phpstan.neon`, **nessuna** modifica al neon.
Host `10.100.200.15` → Pest skip.

## Vincoli

- Fix solo codice PHP/test, mai baseline / ignore / `--level` / neon temp
- Lock per file prima di edit
- Swarm solo su file/moduli disgiunti
- Svuotare `/tmp/phpstan` prima di fidarsi del conteggio

## AC

- [x] Cache phpstan svuotata (`rm -rf /tmp/phpstan`)
- [x] `php -d memory_limit=2G ./vendor/bin/phpstan analyse Modules --memory-limit=2G` eseguito
- [x] Errori corretti nel codice (non soppressi)
- [x] Riverifica `[OK] No errors` (9421 file)
- [x] Pest skip documentato (host 10.100.200.15 = personale2022)

## Esito

**PHPStan:** `[OK] No errors` su 9421 file, exit 0, neon immutato.

Partenza a cache fredda: 29 errori in 6 moduli. Cause e fix (solo codice):

| Area | Problema | Fix |
|------|----------|-----|
| Performance `IndividualeAssenze` / `IndividualeDecurtazioneAssenze` | `query()` phpdoc `|Model` → `filter()` su `Builder\|Model` | `@return Builder<self>`, `self::query()`, guard `instanceof Builder` |
| Progressioni/Ptv `MakePdfAction` | `@var view-string` dopo `view()->exists()` (già narrow) → `varTag.nativeType` | solo `view()->exists` poi `View::make` |
| Ptv `BaseSchedasTable` | filtro ternario generico senza `getName()` su Schema Component | `HaDirittoFilter::make('ha_diritto')` |
| Ptv `PtvBrandLogoPngMagicTest` | `file_get_contents` non Safe | `use function Safe\file_get_contents` |
| Rating `RatingsHostStub` + trait test | `HasMany` TDeclaringModel + payload mixed | `HasMany<MorphPivot, static>` e `array<string, int\|string\|null>` |
| Xot `XotBaseExporterTest` | classe anonima vs childReturnType | stub nominato `ExporterEagerLoadModelStub` |
| Xot `lang/{en,it}/export_xls.php` | chiavi duplicate / marker merge | un solo `label`/`plural_label` |
| IR/Ptv test assenze | `assertIsArray` + override anonimo vs HasMany | Schema mock + foreach regex, niente classe figlia |

**Interferenza fleet:** agenti concorrenti hanno re-iniettato marker `<<<<<<< .merge_file_*` sui file in edit (parse T_SL). Puliti prima del certify. `phpstan.neon` non toccato.

**Pest:** skip. `hostname -I` contiene `10.100.200.15` (personale2022). Nessun `./vendor/bin/pest`.
