---
title: "phpstan analyse Modules — verifica coordinata post merge-markers"
type: story
module: Xot
status: done
story_id: "5.227"
epic: "5"
created: 2026-09-23
updated: 2026-09-23
qmd: "phpstan analyse Modules zero errori verifica coordinata multi-agente merge markers"
related:
  - ./5.181-phpstan-modules-merge-markers-bootstrap.story.md
  - ./5.224-phpstan-analyse-modules.story.md
  - ./5.225-login-logo-merge-markers-parseerror-verified.story.md
---

# 5.227 — `phpstan analyse Modules`: fix + verifica coordinata

## Contesto

Utente: `cd laravel && ./vendor/bin/phpstan analyse Modules` e sistema.
Stesso scope di `5.224` (altro agente attivo in parallelo): coordinamento via
lock + monitoraggio run condivisi, **niente run duplicati** né edit sugli
stessi file mentre l'altro agente li riscriveva.

## Sequenza osservata

1. Run 11:53: bootstrap KO — `ParseError unexpected "<<"` (markers
   `.merge_file_*` in MetatagData; vedi 5.181/5.225).
2. Post-fix markers: primo run a regime → **23 errori** in 10 file
   (test/docblock/generics + 1 lang duplicate key + 1 return type).
3. Fix swarm agente 5.224 sui 10 file; run intermedi 5.181c/d su file in
   movimento (residui/transitori).
4. Verifica per-file (questa story): `HasListaTipoCodiceAspettativeGuardTest`,
   `RatingsHostStub`, `export_xls.{en,it}` → `[OK] No errors` su working tree.
5. Altro agente: commit fix + merge `origin/master` (`6a47b47753`).
6. Run finale completo su `Modules` → vedi Esito.

## Note tecniche

- `RatingsHostStub::$forcedHasMany`: unione `HasMany<MorphPivot,static>|
  HasMany<Rating,static>` — i test assegnano entrambe le forme.
- `HasListaTipoCodiceAspettativeGuardTest`: mock di `Schema::connection()->
  hasTable()` invece di override di `assenze()` → evita il conflitto di firma
  `HasMany<Assenza, $this>` vs `$this` parent (parametro generico invariante).
- `Xot/lang/{en,it}/export_xls.php`: chiave `label` duplicata a livello top.

## AC

- [x] Zero `^<<<<<<< `/`^>>>>>>> ` in `Modules/**/*.php`
- [x] Per-file phpstan su tutti i file flaggati → 0 errori
- [x] `php -d memory_limit=2G ./vendor/bin/phpstan analyse Modules` → `[OK] No errors` (EXIT 0)
- [x] Sprint + memoria aggiornati
- [x] Pest skip documentato (host 10.100.200.15, dati sacri); unit test di scope eseguiti dall'agente 5.181/5.224 (`AssetActionsTest`, `--compact`)

## Esito

`phpstan analyse Modules` → **[OK] No errors** (exit 0) su working tree post-merge
`origin/master`. Due insidie intermedie documentate: (a) run su file in
movimento → parse error transitori, misura solo su edit fermi; (b) il merge
`origin/master` ha reintrodotto markers in 2 test → risolti dal commit
`b6b6c9f15d` prima del run finale pulito.
