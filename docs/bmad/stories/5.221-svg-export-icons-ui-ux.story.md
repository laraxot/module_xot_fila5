---
version: 1.0.0
language: it-IT
title: "Xot — icone SVG custom per export (pdf, xls, xlsx)"
description: "Creazione SVG custom in resources/svg/files/, aggiornamento icone in tutte le azioni di export per migliorare UI/UX"
tags: [svg, icons, export, pdf, xls, xlsx, ui-ux, bmad]
status: done
created_at: 2026-09-23
updated_at: 2026-09-23
---

# Xot — icone SVG custom per export (pdf, xls, xlsx)

## Epic

Miglioramento UI/UX delle azioni di export nel modulo Xot usando icone SVG custom anziché eroicon generici. La regola: `xot-files.{ext}` → `laravel/Modules/Xot/resources/svg/files/{ext}.svg`.

## Acceptance Criteria

- [x] Creati SVG: `resources/svg/files/pdf.svg`, `xls.svg`, `xlsx.svg`
- [x] `ExportPdfAction.php`: `->icon('xot-files.pdf')`
- [x] `ExportXlsAction.php`: `->icon('xot-files.xls')`
- [x] `ExportXlsxAction.php`: `->icon('xot-files.xlsx')`
- [x] `ExportXlsTableAction.php`: `->icon('xot-files.xls')`
- [x] `ExportTreeXlsAction.php`: `->icon('xot-files.xls')`
- [x] `PdfAction.php`: `->icon('xot-files.pdf')`
- [x] `php -l` verde su tutti i file toccati
- [x] PHPStan: 0 errori su tutti i file toccati
- [x] SVG validi (xmllint)
- [x] Regola documentata per future icone custom
- [x] BMAD story e second brain aggiornati

## Regola per icone custom

```
Icon name:   xot-files.{ext}
SVG path:    laravel/Modules/Xot/resources/svg/files/{ext}.svg
Resolution:  BladeUI Icons via XotBaseServiceProvider::registerBladeIcons()
             prefix "xot" → file xot::files.{ext}
```

## Task

1. Creare `resources/svg/files/pdf.svg`, `xls.svg`, `xlsx.svg`
2. Aggiornare icone in tutti i file di export
3. Validare SVG e PHP
4. Documentare regola nei docs del modulo
5. BMAD story + second brain

## Dev Agent Record

### Cosa ho fatto

- Creato 3 SVG files in `resources/svg/files/` (pdf, xls, xlsx) — layout: document outline con lines (consistente con eroicon outline style)
- Aggiornato icone in 6 file (3 Header actions, 2 Table actions, 1 standalone Pdf action)
- Validato SVG con `xmllint`
- Validato PHP con `php -l` e PHPStan

### Evidenze

```bash
php -l Modules/Xot/app/Filament/Actions/Header/ExportPdfAction.php
# No syntax errors detected

phpstan analyse ... --no-progress --memory-limit=-1
# [OK] No errors

xmllint --noout Modules/Xot/resources/svg/files/pdf.svg
# (no output = valid)
```

### Second Brain

- Regola: `xot-files.{ext}` → `resources/svg/files/{ext}.svg`
- Pattern iscritto in `qmd update`

---

**Stato**: done
