---
title: "Story 5.165 (Xot) — celle tipizzate, ordine righe, nome file: un solo Excel per i due canali export"
type: story
module: Xot
epic: "5"
story_id: "5.165"
status: done
track: feature/export
canonical: ../../../Ptv/docs/bmad/stories/5.165-export-parity-cells-order-filename.story.md
qmd: "XotBaseExporter makeXlsxRow xlsxCell NumericCell DefaultValueBinder GetExportFileNameAction ExportXlsAction getFilteredSortedTableQuery no_columns halt XotBaseExportAction fileName"
related:
  - ../../../Ptv/docs/bmad/stories/5.165-export-parity-cells-order-filename.story.md
  - ./5.162-empty-getxlsfields-parity.story.md
  - ./xot-base-exporter.story.md
  - ../../../app/Exports/XotBaseExporter.php
  - ../../../app/Filament/Actions/Header/ExportXlsAction.php
  - ../../../app/Filament/Actions/XotBaseExportAction.php
  - ../../../app/Actions/Export/GetExportFileNameAction.php
  - ../../../tests/Unit/Exports/XotBaseExporterTest.php
  - ../../../tests/Unit/Exports/GetExportFileNameActionTest.php
---

# 5.165 (Xot) — la parte Xot della story Ptv/5.165

Story canonica (perche', tabella divergenze, AC): Ptv/5.165. Qui il lato Xot,
che e' la SSoT dei due canali (`ExportXlsAction` custom, `XotBaseExportAction`
+ `XotBaseExporter` nativo).

## Cosa cambia in Xot

| File | Cambio | Perche' |
|---|---|---|
| `app/Exports/XotBaseExporter.php` | `makeXlsxRow()` override + `xlsxCell()` pubblica statica + `numericCellValue()` | il job nativo passa dal CSV: OpenSpout riceve solo stringhe e `"57"` finiva testo; PhpSpreadsheet (custom) lo scrive numero. Stesso `DefaultValueBinder::dataTypeForValue()` su entrambi |
| `app/Filament/Actions/Header/ExportXlsAction.php` | `getFilteredSortedTableQuery()`; `tableFilters ?? []`; guard `[]` → notifica `no_columns` + `halt()`; nome file da `GetExportFileNameAction`; helper privati (PHPMD CCN 10 → ok) | ordine righe uguale al nativo; Xot/5.162; stesso nome file |
| `app/Filament/Actions/XotBaseExportAction.php` | `fileName()` closure (`Component $livewire`, null se non `ListRecords`) | stesso `{Pagina}-{filtri}` del custom; il downloader aggiunge `.xlsx` |
| `app/Actions/Export/GetExportFileNameAction.php` | nuova | un solo punto per il nome file dei due canali |
| `tests/Unit/Exports/XotBaseExporterTest.php` | describe "celle xlsx tipizzate" (5 test) | AC Ptv/5.165 |
| `tests/Unit/Exports/GetExportFileNameActionTest.php` + `ListRecordsStub.php` | nuovi | nome file = `class_basename` + filtri appiattiti, parita' col custom storico |

Non toccato: `CollectionExport` (il fallback `fields === []` serve
`XlsByModelClassAction`), `IndennitaResponsabilitaExporter` (eredita
`makeXlsxRow` da `XotBaseExporter`: beneficia della stessa tipizzazione).

## Gate

- `php -l`: ok su tutti i file.
- PHPStan max, file toccati: 0 errori (dopo fix `self::` su metodo privato).
- PHPMD `phpmd-ruleset.xml`: 0 su tutti (il primo giro segnalava CCN 10 /
  NPath 288 su `ExportXlsAction::setUp`, risolto estraendo tre helper privati).
- PHPInsights: assente (`vendor/bin/phpinsights` non installato) — skip ambiente.
- Pest: **non eseguito**, DB `10.100.200.53` irraggiungibile (bootstrap
  `DatabaseTransactions` in hang). Verifica con harness standalone che boota
  l'app e ripete le stesse asserzioni dei test Pest piu' un oracolo di parita'
  (`xlsxCell` vs `DefaultValueBinder::dataTypeForValue` su 22 campioni):
  7/7 pass. Da far girare in Ptv/5.156 quando il DB torna.

## Review adversarial → fix (2026-09-23 00:30)

Dettaglio finding nella story canonica Ptv/5.165. Lato Xot:

| File | Cambio |
|---|---|
| `app/Exports/XlsxCellFactory.php` (nuova) | cella OpenSpout = PhpSpreadsheet: `sanitizeUTF8` → `dataTypeForValue` → `0 + $value` / `checkString` (32767, CRLF) |
| `app/Exports/XotBaseExporter.php` | `xlsxCell()` delega alla factory; `CSV_ESCAPE = ''` |
| `app/Exports/Jobs/XotPrepareCsvExport.php` (nuova) | `getExportCsvJob()` → `XotExportCsv`; riscrive `headers.csv` con `CSV_ESCAPE` dopo `parent::handle()` |
| `app/Exports/Jobs/XotExportCsv.php` (nuova) | copia di `ExportCsv::handle()` 5.7.8 + `setEscape`; `incrementEach` al posto di `Expression` non literal |
| `app/Exports/Jobs/XotCreateXlsxFile.php` (nuova) | copia di `CreateXlsxFile::handle()` 5.7.8 + `setEscape` + `includeEmptyRecords`; solo per `XotBaseExporter`, altrimenti `parent::handle()` |
| `app/Filament/Actions/XotBaseExportAction.php` | `->job(XotPrepareCsvExport::class)` |
| `app/Providers/XotServiceProvider.php` | `bind(CreateXlsxFile::class, XotCreateXlsxFile::class)` |
| `tests/Unit/Exports/XotBaseExporterTest.php` | +8 test (overflow, 32767, CRLF/UTF-8, round-trip CSV backslash/quote, chain job, binding) |

Gate: PHPStan max 0 (file toccati + modulo), PHPMD 0 sui file nuovi/toccati
(`XotServiceProvider` coupling 23 preesistente in HEAD), Pint ok. Pest skip
(DB 53 down): harness unit 7/7, review 7/7, E2E 6/6 (9/9 righe identiche sui
due canali, controprova vendor divergente).

Copie vendor: `XotExportCsv::handle` e `XotCreateXlsxFile::handle` vanno
riallineate a ogni bump di `filament/actions` (oggi 5.7.8).

## Chiusura (2026-09-23 00:05)

- Commit: Xot `ced792e8`, Ptv `67c7d01`, root `5fd28a0c1f` — messaggi scritti
  da un agente concorrente sul mio WIP mentre la sessione era ferma per limite
  claude.ai; blob verificati (`git show HEAD:<path>` contiene `xlsxCell`,
  `getFilteredSortedTableQuery`, `fileName`, `modal(false)`), worktree = HEAD.
- `docs/coverage.md` di Xot **non entra in git**: `.gitignore` riga 157
  (`docs/coverage.md`, commit `629b916b` del 2026-09-22 09:17). Misura riportata
  qui: Pest non eseguibile (DB 53 down), harness standalone 7/7, +7 test
  aggiunti (5 `XotBaseExporterTest`, 2 `GetExportFileNameActionTest`). Decisione
  su quella riga di ignore: utente.
- Review adversarial (Workflow 4 lens + 2 verificatori per finding): primo run
  ucciso dal limite di sessione senza risultati, rilanciato al rientro; esito
  nella story Ptv/5.165.
- Remote: laraxot/dev allineato (ahead 0 / behind 0) su Xot, Ptv e root al
  momento del fetch; push eseguito dal peer o da questa sessione, vedi sotto.

## GitHub

Follow-up di https://github.com/laraxot/module_ptv_fila5/issues/15. Issue Xot
non creata in sessione.
