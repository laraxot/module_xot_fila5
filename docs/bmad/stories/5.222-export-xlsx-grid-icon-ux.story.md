---
id: "Xot/5.222-export-xlsx-grid-icon-ux"
title: "ExportXlsx — SVG griglia leggibile a toolbar (no testo XLSX)"
status: done
module: Xot
created: 2026-09-23
related:
  - ./5.221-svg-export-icons-ui-ux.story.md
  - ./5.221-export-actions-xot-files-icon-set.story.md
  - ./5.220-export-xlsx-svg-icon-swap.story.md
  - ../../svg-icon-custom.md
qmd: "xot-files.xlsx svg grid icon ui ux export filament blade icons no tiny text"
---

# Story: icona ExportXlsx migliore (UI/UX)

## Perché

`xot-files.xlsx` puntava già al SVG custom, ma il disegno usava
`<text>XLSX</text>` a font-size 4: a 16–24px in header Filament è
illeggibile e non comunica "foglio di calcolo".

## Regola (SSoT)

`docs/svg-icon-custom.md`: `xot-files.{ext}` → `resources/svg/files/{ext}.svg`.
Preferire **path geometrici** (documento + griglia), non testo microscopico.

## AC

- [x] `xlsx.svg` = foglio + header pieno + griglia 3×2 (path only)
- [x] `xls.svg` / `pdf.svg` allineati (distinguibili, stesso linguaggio visuale)
- [x] `ExportXlsxAction` resta `->icon('xot-files.xlsx')`
- [x] Pest: assert icona + file SVG esiste
- [x] `xmllint` + PHPStan scope action

## Esito

Gate nella chiusura sessione.

## Gate

- xmllint pdf/xls/xlsx: ok
- PHPStan ExportXlsxAction + test: vedi sessione
- Pest ExportXlsxActionTest: vedi sessione

## Prompt 04.md (righe 4–7) — verificato 2026-09-23

Tutte le action export usano `xot-files.*` + SVG in `resources/svg/files/`.
Nessun heroicon generico su Pdf/Xls/Xlsx header. Follow-up: `ExportXlsLazyAction`
+ `PdfAction` allineati (icona non via `__()`).

## Gate Pest (2026-09-23)

`9 passed (38 assertions)` — ExportPdf/Xls/Xlsx + Lazy + PdfAction table.
PHPStan scope Lazy+PdfAction: `[OK] No errors`.
